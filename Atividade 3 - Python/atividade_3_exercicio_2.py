# Fale devagar
resposta = input("Escreva uma frase.") # Pede uma string ao usuário.
print(resposta.split()) # Divide a string, com o caractere selecionado. (não está funcionando, só o caractere padrão :c)
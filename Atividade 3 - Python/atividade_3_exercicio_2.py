# Fale devagar
resposta = input("Escreva uma frase.") # Pede uma string ao usuário.
print(resposta.split()) # Divide a string, com o carácter selecionado. (não está funcionando, só o carácter padrão :c)